# Copy to .env and fill in. Never commit .env — only .env.example.

# --- Postgres ---
POSTGRES_USER=aml_app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=aml_investigation
POSTGRES_HOST=localhost
POSTGRES_PORT=5434

# --- AMLSim ---
# Absolute path to the AMLSim outputs directory containing accounts.csv,
# transactions.csv, alert_transactions.csv for the current simulation run.
# Kept in .env (not hardcoded) so different machines or regenerated
# simulations (different simulation_name) work without code changes.
AMLSIM_OUTPUT_DIR=C:/Users/zahoo/Desktop/AI-Engineering Projects/AMLSim/AMLSim/outputs/aml_10k_sa
